name: serverpod_boost
version: 0.1.0
description: >-
  AI acceleration for ServerPod development via MCP (Model Context Protocol).
  Provides AI assistants with deep context awareness of ServerPod projects,
  enabling high-quality code generation through semantic understanding.
homepage: # TODO: Set actual homepage URL
repository: # TODO: Set actual repository URL
issue_tracker: # TODO: Set actual issue tracker URL
documentation: # TODO: Set actual documentation URL

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  serverpod: ^3.2.3
  yaml: ^3.1.2
  path: ^1.8.3
  mustache_template: ^2.0.0
  args: ^2.4.2
  console: ^4.1.0
  http: ^1.6.0
  postgres: ^3.0.0
  mcp_server: ^1.0.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.5

topics:
  - serverpod
  - mcp
  - ai
  - code-generation
  - development-tools

executables:
  boost:
