# ServerPod Boost Visual Architecture Guide

## Project Structure Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          SERVERPOD MONOREPO                                  │
│                         /Users/musinsa/always_summer/pilly                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                        │
        ┌───────────────────────────────┼───────────────────────────────┐
        │                               │                               │
        ▼                               ▼                               ▼
┌─────────────────┐           ┌─────────────────┐           ┌─────────────────┐
│  pilly_server   │           │  pilly_client   │           │  pilly_flutter  │
│  (SERVER)       │◄──────────│  (CLIENT)       │◄──────────│  (FLUTTER APP)  │
│                 │  GENERATE │                 │  DEPENDS  │                 │
└─────────────────┘           └─────────────────┘           └─────────────────┘
```

## Detailed Architecture Diagram

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ pilly_server/                     │ SOURCE OF TRUTH                            │
├────────────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  config/                                                                │
│  ├── generator.yaml ⚡  →  client_package_path: ../pilly_client          │
│  ├── development.yaml    →  API server config (port 8080)                │
│  └── passwords.yaml      →  Database credentials                         │
│                                                                           │
│  lib/src/                                                                │
│  ├── greetings/                                                          │
│  │   └── greeting_endpoint.dart  ✏️  YOU WRITE THIS                      │
│  │       └── GreetingEndpoint                                             │
│  │           └── hello(name: String): Future<Greeting>                   │
│  │                                                                      │
│  ├── auth/                                                              │
│  │   ├── email_idp_endpoint.dart  (Auth provided by ServerPod)          │
│  │   └── jwt_refresh_endpoint.dart                                      │
│  │                                                                      │
│  └── generated/  🤖  DO NOT EDIT                                         │
│      ├── protocol.yaml          →  Endpoint/method listing               │
│      ├── endpoints.dart         →  Runtime dispatcher                    │
│      ├── protocol.dart          →  Serialization manager                 │
│      └── greetings/                                                        │
│          └── greeting.dart      →  Generated Greeting model              │
│                                                                          │
│  migrations/  →  Database schema changes                                 │
│  test/        →  Server tests                                            │
└────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ serverpod generate
                                    │ (reads generator.yaml)
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ pilly_client/                      │ GENERATED (DO NOT EDIT)                   │
├────────────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  lib/src/protocol/  🤖  ALL GENERATED                                    │
│  ├── protocol.dart              →  Client serialization                    │
│  ├── client.dart                →  Generated client methods                │
│  │       ├── EndpointGreeting                                            │
│  │       │   └── hello(name): Future<Greeting>  ← Generated method       │
│  │       ├── EndpointEmailIdp                                            │
│  │       └── EndpointJwtRefresh                                          │
│  └── greetings/                                                        │
│      └── greeting.dart          →  Generated Greeting model               │
│                                                                          │
│  lib/pilly_client.dart  →  Main export file                              │
└────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ pubspec.yaml:
                                    │   pilly_client:
                                    │     path: ../pilly_client
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ pilly_flutter/                     │ FRONTEND APPLICATION                     │
├────────────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  lib/                                                                   │
│  ├── main.dart  ✏️  YOU WRITE THIS                                      │
│  │   └── client = Client(serverUrl)  ← Initialize from pilly_client      │
│  │                                                                      │
│  └── screens/                                                           │
│      ├── greetings_screen.dart  ✏️  YOU WRITE THIS                       │
│      │   └── client.greeting.hello('World')  ← Use generated client      │
│      └── sign_in_screen.dart       (Auth provided by ServerPod)         │
│                                                                          │
│  assets/                                                                │
│  └── config.json  →  serverUrl: "http://localhost:8080/"                │
│                                                                          │
│  ios/, android/, web/, macos/, linux/  → Platform code                  │
└────────────────────────────────────────────────────────────────────────────────┘
```

## Code Generation Flow

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: DEVELOPER WRITES ENDPOINT                                           │
└────────────────────────────────────────────────────────────────────────────────┘

File: pilly_server/lib/src/greetings/greeting_endpoint.dart

┌────────────────────────────────────────────────────────────────────────────────┐
│ import '../generated/protocol.dart';                                         │
│ import 'package:serverpod/serverpod.dart';                                   │
│                                                                              │
│ class GreetingEndpoint extends Endpoint {                                    │
│   Future<Greeting> hello(Session session, String name) async {              │
│     return Greeting(                                                         │
│       message: 'Hello $name',                                               │
│       author: 'Serverpod',                                                  │
│       timestamp: DateTime.now(),                                            │
│     );                                                                       │
│   }                                                                          │
│ }                                                                            │
└────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: RUN CODE GENERATION                                                  │
└────────────────────────────────────────────────────────────────────────────────┘

$ cd pilly_server
$ serverpod generate

┌────────────────────────────────────────────────────────────────────────────────┐
│  ▶ Scanning lib/src/ for endpoints...                                        │
│  ▶ Found 3 endpoints: emailIdp, jwtRefresh, greeting                         │
│  ▶ Parsing endpoint methods...                                               │
│  ▶ Generating server code...                                                 │
│     ✓ lib/src/generated/protocol.yaml                                        │
│     ✓ lib/src/generated/endpoints.dart                                       │
│     ✓ lib/src/generated/protocol.dart                                        │
│  ▶ Reading client_package_path from config/generator.yaml...                │
│     → ../pilly_client                                                        │
│  ▶ Generating client code...                                                 │
│     ✓ ../pilly_client/lib/src/protocol/protocol.dart                        │
│     ✓ ../pilly_client/lib/src/protocol/client.dart                          │
│  ▶ Done!                                                                     │
└────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: GENERATED SERVER CODE (pilly_server/lib/src/generated/)             │
└────────────────────────────────────────────────────────────────────────────────┘

endpoints.dart:
┌────────────────────────────────────────────────────────────────────────────────┐
│ class Endpoints extends EndpointDispatch {                                   │
│   @override                                                                  │
│   void initializeEndpoints(Server server) {                                  │
│     var endpoints = <String, Endpoint>{                                      │
│       'greeting': GreetingEndpoint()                                         │
│         ..initialize(server, 'greeting', null),                              │
│       // ... other endpoints                                                 │
│     };                                                                        │
│     // ... method connectors for reflection                                  │
│   }                                                                          │
│ }                                                                            │
└────────────────────────────────────────────────────────────────────────────────┘

protocol.yaml:
┌────────────────────────────────────────────────────────────────────────────────┐
│ greeting:                                                                    │
│   - hello:                                                                   │
│ emailIdp:                                                                    │
│   - login:                                                                   │
│   - startRegistration:                                                       │
│   # ...                                                                      │
└────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: GENERATED CLIENT CODE (pilly_client/lib/src/protocol/)              │
└────────────────────────────────────────────────────────────────────────────────┘

client.dart:
┌────────────────────────────────────────────────────────────────────────────────┐
│ class EndpointGreeting {                                                     │
│   final EndpointCaller caller;                                               │
│                                                                              │
│   EndpointGreeting(this.caller);                                             │
│                                                                              │
│   Future<Greeting> hello(String name) async {                                │
│     return caller.callServerEndpoint<Greeting>(                              │
│       'greeting',                                                            │
│       'hello',                                                               │
│       {'name': name},                                                        │
│     );                                                                       │
│   }                                                                          │
│ }                                                                            │
└────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ STEP 5: USE IN FLUTTER APP                                                   │
└────────────────────────────────────────────────────────────────────────────────┘

File: pilly_flutter/lib/screens/greetings_screen.dart

┌────────────────────────────────────────────────────────────────────────────────┐
│ import '../main.dart';  // imports 'client'                                 │
│                                                                              │
│ void _callHello() async {                                                    │
│   try {                                                                      │
│     final result = await client.greeting.hello(_textEditingController.text); │
│     //                      ^^^^^^^ ^^^^^^^ ^^^^^                            │
│     //                      |       |      |                                 │
│     //                      |       |      └─ Parameter (type-checked!)      │
│     //                      |       └─ Generated method (autocomplete!)      │
│     //                      └─ Generated endpoint module                     │
│                                                                              │
│     setState(() {                                                            │
│       _resultMessage = result.message;  // Type-safe access!                │
│     });                                                                      │
│   } catch (e) {                                                              │
│     setState(() {                                                            │
│       _errorMessage = '$e';                                                  │
│     });                                                                      │
│   }                                                                          │
│ }                                                                            │
└────────────────────────────────────────────────────────────────────────────────┘
```

## Type Safety Flow

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ SERVER: Define Model                                                         │
└────────────────────────────────────────────────────────────────────────────────┘

class Greeting implements SerializableModel {
  String message;
  String author;
  DateTime timestamp;
}

                                    │
                                    │ serverpod generate
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ GENERATED: Client & Server get same model                                     │
└────────────────────────────────────────────────────────────────────────────────┘

Server: lib/src/generated/greetings/greeting.dart
Client: lib/src/protocol/greetings/greeting.dart

Both contain:
┌────────────────────────────────────────────────────────────────────────────────┐
│ abstract class Greeting implements SerializableModel {                        │
│   factory Greeting({                                                          │
│     required String message,                                                  │
│     required String author,                                                  │
│     required DateTime timestamp,                                             │
│   }) = _GreetingImpl;                                                         │
│                                                                              │
│   String message;                                                             │
│   String author;                                                              │
│   DateTime timestamp;                                                         │
│ }                                                                            │
└────────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│ FLUTTER: Type-safe usage                                                     │
└────────────────────────────────────────────────────────────────────────────────┘

final greeting = await client.greeting.hello('World');

print(greeting.message);     // ✓ Compile-time checked
print(greeting.author);      // ✓ Compile-time checked
print(greeting.timestamp);   // ✓ Compile-time checked
print(greeting.foo);         // ✗ Compile-time error!
```

## ServerPod Boost Integration Points

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ SERVERPOD BOOST SHOULD INSTALL IN SERVER PACKAGE                              │
└────────────────────────────────────────────────────────────────────────────────┘

pilly_server/
├── .ai/                              ← BOOST INSTALLATION HERE
│   └── boost/
│       ├── core.dart.md             ← ServerPod fundamentals
│       ├── server/                   ← Server-specific skills
│       │   ├── endpoints.md
│       │   ├── models.md
│       │   └── migrations.md
│       ├── client/                   ← Client understanding
│       │   └── usage.md
│       └── flutter/                  ← Flutter integration
│           └── client-integration.md
│
├── config/
│   └── generator.yaml                ← BOOST READS THIS
│
└── lib/src/
    ├── [your endpoints]              ← BOOST HELPS CREATE THESE
    └── generated/                    ← BOOST VALIDATES THIS
```

## Development Workflow Comparison

### WITHOUT ServerPod Boost

```
1. Create directory: lib/src/users/
2. Create file: users_endpoint.dart
3. Write endpoint class
4. Write model classes
5. cd to server package
6. Run: serverpod generate
7. Check for errors
8. Fix errors manually
9. Run serverpod generate again
10. Go to Flutter app
11. Import client package
12. Write UI code
13. Test manually
```

### WITH ServerPod Boost

```
1. boost create:endpoint Users --methods:list,get,create
   → Creates endpoint file
   → Generates method stubs
   → Creates model templates
   → Runs serverpod generate
   → Validates output
   → Reports success

2. boost run --hot-reload
   → Starts server with hot reload
   → Runs Flutter app
   → Shows both logs
   → Tests endpoints automatically

3. boost test
   → Runs server tests
   → Runs Flutter tests
   → Reports coverage
```

## Key Files for ServerPod Boost

### Must Read
- `/server/config/generator.yaml` - Find client package location
- `/server/lib/src/generated/protocol.yaml` - Endpoint listing
- `/server/pubspec.yaml` - Server dependencies
- `/client/pubspec.yaml` - Client dependencies
- `/flutter/pubspec.yaml` - Flutter dependencies

### Must Monitor
- `/server/lib/src/**/*_endpoint.dart` - Endpoint definitions
- `/server/lib/src/generated/` - Generated server code
- `/client/lib/src/protocol/` - Generated client code
- `/server/migrations/` - Database migrations

### Must Generate
- Endpoint scaffolds
- Model templates
- Test stubs
- Migration files

## MCP Tools Architecture

```
┌────────────────────────────────────────────────────────────────────────────────┐
│ MCP SERVER: boost                                                            │
├────────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Tools:                                                                      │
│  ├── generate_protocol    → Wraps 'serverpod generate'                      │
│  ├── create_endpoint       → Scaffold new endpoint                          │
│  ├── create_model          → Scaffold new model                             │
│  ├── list_endpoints        → Show all endpoints                             │
│  ├── validate_protocol     → Check before generation                        │
│  ├── run_tests             → Run server & flutter tests                     │
│  ├── start_server          → Start dev server                               │
│  └── get_schema            → Show current protocol schema                   │
│                                                                              │
│  Resources:                                                                  │
│  ├── endpoints            → All endpoint definitions                        │
│  ├── models               → All data models                                 │
│  ├── migrations           → Database migrations                             │
│  └── config               → Server configuration                            │
│                                                                              │
└────────────────────────────────────────────────────────────────────────────────┘
```

## Success Criteria

ServerPod Boost is successful when:

1. **Detection** ✓
   - Automatically finds all 3 packages
   - Validates package structure
   - Reads generator.yaml correctly

2. **Generation** ✓
   - Wraps `serverpod generate`
   - Validates before generating
   - Reports changes clearly

3. **Scaffolding** ✓
   - Creates endpoint files
   - Generates method stubs
   - Creates model templates

4. **Documentation** ✓
   - Provides ServerPod-specific skills
   - Explains best practices
   - Shows examples

5. **Testing** ✓
   - Runs server tests
   - Runs Flutter tests
   - Reports coverage

6. **Developer Experience** ✓
   - Type-safe autocomplete
   - Clear error messages
   - Fast feedback loop
```

This visual guide should be used alongside:
- `PILLY_ANALYSIS.md` - Complete technical analysis
- `PACKAGE_DETECTION.md` - Detection algorithm
- `CODE_GENERATION_FLOW.md` - Generation details
